export enum ContactType {
  LEAD = "lead",
  COMPANY = "company",
  SALES = "sales",
}

export enum AccountStatus {
  HOT = 1,
  WARM = 2,
  COLD = 3,
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum AccountType {
  COMPANY = "company",
  SALES = "sales",
}

export enum ActionType {
  CALL = "call",
  MEETING = "meeting",
  TEXT = "text",
}
